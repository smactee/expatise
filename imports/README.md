Temporary screenshot intake lives here.

- `imports/<lang>/raw/` is for raw reference screenshots only.
- `imports/<lang>/batch-###/` is for one reviewed processing batch at a time.
- Screenshots in this tree must never be copied into production app assets.
