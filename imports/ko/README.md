Korean screenshot intake scaffold.

- Put raw screenshot references in `imports/ko/raw/`.
- Batch-specific extraction data belongs in `imports/ko/batch-001/intake.json`.
- Generated batch outputs stay beside the intake file as `matched.json`, `review-needed.json`, and `unresolved.json`.
